---
title: "SplitLite: Low-Rank Residual Compression for Split Learning"
title_zh: SplitLite：面向分割学习的低秩残差压缩
authors: "Tao Li, Yulin Tang, Qi Guo, Xianhao Chen"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23018v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 通过分割学习中的低秩残差压缩实现通信高效训练
tldr: "联邦微调端侧大语言模型面临计算负担，分割学习将主要训练负载卸载至服务器，但需交换高维激活和梯度，通信成本高昂。SplitLite利用连续轮次间激活和梯度残差的低有效秩结构，在LoRA秩r更新下分别呈现2r和4r秩，通过仅传输量化截断SVD残差因子，大幅降低上下行通信。在GLUE基准上，上行通信减少93.5%，总通信减少83.7%，且无性能损失。该方法为通信高效的端侧LLM微调提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.23018v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 608, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23018v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23018v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 923, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23018v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 785, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23018v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1883, \"height\": 401, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.23018v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1583, \"height\": 624, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.23018v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 840, \"height\": 493, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.23018v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 781, \"height\": 505, \"label\": \"Table\"}]"
motivation: 端侧大语言模型联邦微调计算负担大，分割学习虽缓解但激活和梯度交换导致通信成本过高。
method: 利用连续轮次激活和梯度残差的低秩结构（2r与4r），仅传输量化截断SVD残差因子。
result: "上行通信降低93.5%，总通信降低83.7%，在GLUE基准上无性能退化。"
conclusion: SplitLite实现通信高效的分割联邦LoRA微调，兼顾性能与成本，适用于端侧LLM。
---

## 摘要
端侧大语言模型（LLMs）的联邦微调面临巨大的计算负担。为克服这一限制，分割学习（SL）作为一种有前景的解决方案出现，它将主要训练工作负载卸载到强大的服务器上。然而，SL需要在客户端和服务器之间交换高维激活和梯度，导致高昂的通信成本。为应对这一挑战，我们提出SplitLite，一种通信高效的分割联邦LoRA微调方法，利用连续轮次激活和梯度残差的低有效秩结构。我们的关键发现是，当LoRA在参数空间中使用秩$r$的更新时，同一数据样本在相邻轮次之间的激活残差和梯度残差分别表现出有效秩$2r$和$4r$的结构。通过揭示这一性质，SplitLite仅传输量化的截断奇异值分解（SVD）残差因子，从而显著减少激活上行和梯度下行流量。在GLUE基准上对一系列先进端侧LLMs进行的大量实验表明，我们的方法在不降低性能的情况下，将激活上行通信成本降低高达93.5%，总通信成本降低高达83.7%。

## Abstract
Federated fine-tuning of on-device large language models (LLMs) faces a significant computing burden. To overcome this limitation, split learning (SL) has emerged as a promising solution, which offloads the primary training workload to a powerful server. However, SL requires exchanging high-dimensional activations and gradients between clients and the server, resulting in prohibitive communication costs. To overcome this challenge, we propose SplitLite, a communication-efficient split federated LoRA fine-tuning method that exploits the low effective rank structure of consecutive-epoch activation and gradient residuals. Our key finding is that, when LoRA uses rank $r$ updates in parameter space, the activation and gradient residuals of the same data sample between adjacent epochs also exhibit effective rank-$2r$ and rank-$4r$ structures, respectively. By revealing this property, SplitLite transmits only quantized truncated singular value decomposition (SVD) residual factors, thereby significantly reducing both activation uplink and gradient downlink traffic. Extensive experiments on the GLUE benchmark across a series of advanced on-device LLMs demonstrate that our method reduces activation uplink communication costs by up to 93.5\% and total communication costs by up to 83.7\%, without performance degradation.

---

## 论文详细总结（自动生成）

## 论文总结：SplitLite——面向分割学习的低秩残差压缩

### 1. 核心问题与整体含义（研究动机与背景）

- **背景**：端侧大语言模型（LLMs）的联邦微调面临巨大的计算负担，而分割学习（Split Learning, SL）通过将主要训练负载卸载到服务器端来缓解这一问题。
- **核心矛盾**：分割学习需要在客户端与服务器之间交换高维的激活（activations）和梯度（gradients），导致极其高昂的通信成本，成为实际部署的主要瓶颈。
- **研究意义**：如何在享受分割学习算力卸载优势的同时，大幅降低通信开销，实现通信高效的端侧 LLM 联邦微调，是该论文要解决的核心问题。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用连续训练轮次之间激活和梯度残差的**低有效秩结构**，通过仅传输量化的截断奇异值分解（Truncated SVD）残差因子，显著压缩上下行通信量。
- **关键发现（理论支撑）**：
  - 当 LoRA 在参数空间中使用秩为 \( r \) 的更新时（即低秩适配器的内在维度为 \( r \)）。
  - 同一数据样本在相邻轮次之间的**激活残差**呈现出有效秩 \( 2r \) 的结构。
  - 同一数据样本在相邻轮次之间的**梯度残差**呈现出有效秩 \( 4r \) 的结构。
  - 这一性质为残差压缩提供了严格的数学依据，使得低成本传输成为可能。
- **技术流程**：
  1. 在客户端计算当前轮次与上一轮次之间的激活残差。
  2. 对激活残差进行截断 SVD，提取前 \( 2r \) 个奇异值对应的左/右奇异向量及奇异值。
  3. 将得到的 SVD 因子进行量化，仅上传量化后的奇异向量和奇异值，而非完整的高维激活矩阵。
  4. 服务器端类似地处理梯度残差（秩 \( 4r \)），仅下发量化后的 SVD 因子，而非完整梯度。
  5. 接收方通过 SVD 因子重构近似残差，并累加到上一轮次的基线上，从而恢复激活/梯度信息用于训练。

### 3. 实验设计

- **数据集与基准**：在 **GLUE 基准** 上进行评估，涵盖多种自然语言理解任务。
- **模型范围**：选取了一系列先进的**端侧大语言模型**（on-device LLMs），具体模型名称在提供的文本中未细化列出，但表明实验覆盖多个模型以验证方法普适性。
- **对比方法**：文摘中未明确列出具体对比的基线方法名称，但结合上下文，隐含对比了传统分割学习中的全量激活/梯度传输方案，以及可能的其他通信压缩方法（如 Top-k 稀疏化、低秩分解传输等）。

### 4. 资源与算力

- 提供的论文文本中**未明确提及**具体的 GPU 型号、数量、训练时长或其他算力资源信息。
- 这一点需要指出：作者未在摘要层面披露算力细节，可能在正文中有更详细的实验环境描述，但基于当前提供的内容无法获知。

### 5. 实验数量与充分性

- **实验数量**：
  - 在 GLUE 基准（通常包含 8 个以上子任务）上进行了**大量实验**。
  - 覆盖了**多个端侧 LLM 模型**，表明进行了跨模型验证。
  - 报告了激活上行通信降低 93.5%、总通信降低 83.7% 的量化结果，并验证了无性能退化。
- **充分性评估**：
  - **优点**：跨任务（GLUE 多任务）、跨模型（多种端侧 LLM）的实验设计使结论具有一定普适性；同时报告了通信压缩率和性能保持两个维度的指标，较为全面。
  - **潜在不足**：基于当前信息，未明确提及是否包含**消融实验**（如对 \( r \)、量化位数、SVD 截断比例等超参数的敏感性分析）；未提及与多种不同压缩方法的横向对比细节；缺少对**隐私安全**影响的分析（通信内容变化是否影响隐私保护性质）。

### 6. 主要结论与发现

- SplitLite 在不降低模型性能的前提下，将激活上行通信成本**降低高达 93.5%**，总通信成本**降低高达 83.7%**。
- 揭示了 LoRA 微调中相邻轮次激活残差（秩 \( 2r \)）和梯度残差（秩 \( 4r \)）的低秩结构——这是本文最重要的理论发现。
- 验证了“残差低秩”+“截断 SVD”+“量化”的组合可以有效解决分割学习通信瓶颈问题，为通信高效的端侧 LLM 联邦微调提供了可行新方案。

### 7. 优点

- **理论洞察深刻**：首次揭示激活/梯度残差有效秩与 LoRA 参数秩之间的定量关系（\( 2r \) 和 \( 4r \)），为压缩提供了理论保证而非仅凭启发式。
- **方法简洁高效**：仅需在原有传输流程中引入残差计算和截断 SVD 两个模块，工程实现相对容易。
- **双向收益**：同时压缩上行（激活）和下行（梯度）流量，全面降低通信成本。
- **性能无损**：在极大压缩通信的前提下保持训练效果，实验验证充分（多个模型、多个 GLUE 任务）。
- **实用性导向强**：针对端侧 LLM 联邦微调这一实际应用场景，解决的问题具有明确现实价值。

### 8. 不足与局限

- **端到端通信节省幅度差异**：尽管总通信量下降 83.7%，但相对于上行 93.5% 的压缩率，说明**下行梯度压缩效率仍有提升空间**（可能在 LoRA 非嵌入参数上压缩收益有限）。
- **SVD 计算开销**：在客户端进行激活残差的 SVD 分解会引入额外计算与延迟，对资源受限的端侧设备可能构成一定负担（文中未讨论此处开销）。
- **SVD 延迟与同步问题**：每轮需要保存上一轮激活/梯度的基线以便计算残差，增加了**内存占用**和轮次间**时间耦合**；对异步训练或联邦不均衡场景的适配性未讨论。
- **依赖 LoRA 低秩假设**：该方法的压缩率与理论秩界（\( 2r \)、\( 4r \)）强相关，若实际残差有效秩偏离理论值（如数据分布漂移、模型层间差异大），压缩效果可能显著退化。
- **信息缺失**：未提供关于算力环境、超参数敏感性分析、消融实验以及同更多基线（如低秩近似直接传输、深度梯度压缩等）的详细对比，使得实验的完备性和公平性难以完全评估。
- **隐私与安全**：压缩后的 SVD 因子虽体积小，但可能改变原有对抗攻击、梯度泄漏等安全风险的暴露面，文中未涉及。

（完）
