---
title: When Is Shallow Enough? Adaptive Split Federated Learning with Client-Specific Sufficiency Estimation
title_zh: 何时浅层足够？基于客户端充分性估计的自适应分割联邦学习
authors: "Wenhao Yuan, Chenchen Lin, Wenhao Hu, Jian Chen, Jinfeng Xu, Shujie Li, Edith Cheuk Han Ngai"
date: 2026-08-16
pdf: "https://arxiv.org/pdf/2608.15639v1"
tags: ["query:dgnn-nas"]
score: 8.0
evidence: 自适应分割联邦学习，减少通信开销
tldr: 传统的分割联邦学习采用静态分割点，在客户端异构场景下难以适应不同客户端的数据分布与训练进度，导致性能次优且计算浪费。为此提出FedSGA框架，通过客户端特定提示令牌跟踪适应动态，并设计浅层充分性估计器预判浅层分割是否足够，同时利用接口协调模块统一不同深度特征。实验在多个异构基准上验证了FedSGA能有效提升模型性能并减少不必要的客户端计算，展现了自适应分割的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.15639v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1816, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15639v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1802, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15639v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1800, \"height\": 330, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15639v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 866, \"height\": 347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.15639v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 552, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 887, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1847, \"height\": 549, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1846, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1835, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1842, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1837, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15639v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1837, \"height\": 216, \"label\": \"Table\"}]"
motivation: 静态分割在客户端异构下不优，单一分割点难以适配不同客户端的数据分布与训练进度。
method: 提出FedSGA，含提示令牌适应通道、浅层充分性估计器和接口协调模块，实现自适应分割。
result: 在多个异构基准上优于现有SOTA方法，同时减少不必要的客户端计算开销。
conclusion: 通过充分性估计自适应分割深度，在提升模型性能的同时降低客户端计算负担，实现性能与效率兼顾。
---

## 摘要
分割联邦学习（SFL）通过在服务器和客户端之间分割网络来实现分布式模型训练。然而，在客户端异质性下，传统的静态分割策略可能不是最优的，因为客户端在数据分布、自适应动态和表示学习进度方面可能存在差异，使得单一分割点不足以适应客户端特定的训练状态。在本文中，我们提出了\textsc{FedSGA}，一种基于充分性引导的自适应分割联邦学习框架，通过客户端特定的浅层充分性估计来解决这个问题。首先，我们引入了一个基于私有提示令牌的客户端特定自适应通道，该通道在共享骨干网络之外单独跟踪局部自适应动态，并提供一个轻量级信号来检测客户端自适应是否仍然活跃。为了进一步避免对多个候选深度进行重复在线探测，我们设计了一个浅层充分性估计器，该估计器结合了跨客户端语义对齐、时间接口稳定性和提示状态变化来估计最浅层分割是否已经足够。最后，我们引入了一个分割兼容的接口协调模块，将来自不同分割深度的激活投影到一个共享语义空间中，从而在服务器端预测之前提高异构客户端接口的可比性。在多个异构基准上的大量实验表明，\textsc{FedSGA}在提高模型性能方面优于最先进的方法，同时减少了不必要的客户端计算。

## Abstract
\textit{Split Federated Learning} (SFL) enables distributed model training by splitting networks between the server and clients. However, under client heterogeneity, the conventional static split strategy may be suboptimal because clients can differ in data distributions, adaptation dynamics, and representation learning progress, making a single split point insufficient to accommodate client-specific training states. In this paper, we propose \textsc{FedSGA}, a \textbf{S}ufficiency-\textbf{G}uided \textbf{A}daptive split \textbf{Fed}erated learning framework that addresses this question through client-specific shallow sufficiency estimation. First, we introduce a client-specific adaptation channel based on private prompt tokens, which tracks local adaptation dynamics separately from the shared backbone and provides a lightweight signal for detecting whether client adaptation remains active. To further avoid repeated online probing over multiple candidate depths, we design a shallow sufficiency estimator that combines cross-client semantic alignment, temporal interface stability, and prompt-state variation to estimate whether the shallowest split is already sufficient. Finally, we introduce a split-compatible interface harmonization module that projects activations from different split depths into a shared semantic space, improving the comparability of heterogeneous client interfaces before server-side prediction. Extensive experiments on multiple heterogeneous benchmarks demonstrate the effectiveness of \textsc{FedSGA} in improving model performance compared with state-of-the-art methods while reducing unnecessary client-side computation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

分割联邦学习（Split Federated Learning, SFL）通过在服务器与客户端之间分割模型，使客户端只需执行前向网络的一部分，从而降低客户端计算负担。然而，现有SFL方法普遍采用**对所有客户端统一的静态分割点**，这一隐含假设在客户端异构场景下难以成立：

- 不同客户端在**数据分布**（如标签偏移、域偏移）、**局部适应动态**和**表示学习进度**上存在显著差异，单一分割点无法适配各客户端的个性化训练状态。
- 客户端异构性在分割接口处被进一步放大：不同客户端暴露给服务器的中间激活可能具有不同的语义成熟度、域相关特征偏差或不兼容的表示几何结构，导致共享的服务器端模型难以有效整合对齐。
- 现有自适应分割策略要么基于**资源驱动**（如设备能力、延迟、通信成本），无法反映接口表示是否包含足够的任务相关信息；要么通过**在线探测**多个候选深度来评估分割适宜性，但重复探测带来不可忽视的计算开销。

**核心研究问题**：SFL如何根据客户端特定的接口可靠性自适应调整分割点，同时保持异构客户端间的服务器端学习兼容性？论文将该问题形式化为**客户端特定浅层充分性估计**（client-specific shallow sufficiency estimation），即判断每个客户端的最浅层分割接口是否已包含足够的任务相关信息，以支持协作式服务器端训练。

## 2. 方法论

### 2.1 核心思想

论文提出 **FedSGA**（Sufficiency-Guided Adaptive Split Federated Learning）框架，核心洞察是：浅层接口的质量不仅取决于其当前的表示质量，还取决于客户端局部适应是否已充分稳定。框架由三个关键模块组成：

### 2.2 客户端特定适应通道（Client-Specific Adaptation Channel）

- 为每个客户端维护一组**私有提示令牌（prompt tokens）** M_i，与共享骨干网络解耦，不参与跨客户端聚合。
- 利用一阶贡献分数 c_t_{i,k} = |(∂ℓ_t_i / ∂ε_{i,k})ᵀ ε_{i,k}| 衡量每个提示令牌对局部目标的敏感度，并通过重要性加权聚合为适应描述子 m_t_i（式2）。
- 提示令牌仅通过客户端局部目标更新，其演化过程提供了更干净的客户端局部适应状态视图；描述子的时间变化可作为指示客户端适应是否仍在进行的轻量级信号。

### 2.3 浅层充分性估计器（Shallow Sufficiency Estimator）

为替代对多个候选深度的重复在线探测，论文设计了一个低容量**单调模型** Γ_θ，基于以下三类观测信号估计最浅层分割的充分性概率 r_t_i ∈ [0,1]：

- **跨客户端语义对齐** a_t_i：浅层表示与服务器端类原型之间的余弦相似度。
- **时间接口稳定性** δ_t_i：浅层表示在不同通信轮次间的变化量。
- **提示状态变化** q_t_i：适应描述子的逐轮变化幅度。

利用**稀疏校准监督**（Alg. 1）：在选定的校准轮次中，对比最浅分割 p₁ 与固定中等深度参考分割 p_ref 的损失，构造二值目标 s_t_i = 1[L(p₁) ≤ (1+β)L(p_ref)] 来训练估计器。最终通过预算感知单调策略实现离散分割深度选择（式8）：p̂_t_i = p_k, k = min{K, max{1, ⌈K(1−r_t_i+ρ)⌉}}。

### 2.4 分割兼容接口协调模块（Split-Compatible Interface Harmonization）

- 由于自适应分割使不同客户端在不同深度离线，服务器接收的激活具有异构语义粒度。
- 引入**深度感知投影器** Z = φ(z̃, e(p̂))，将不同分割深度的激活映射到共享语义空间，深度嵌入提供分割位置的显式信息以补偿语义层级的系统性偏移。
- 通过服务器端维护的**类原型**进行正则化，原型对齐损失（式9）鼓励不同深度、不同客户端状态的投影表示保持一致的类级几何结构，整体目标函数如式10所示。

### 2.5 算法流程

整体训练过程如Alg. 2所示：每轮中，参与客户端计算观测信号，训练/评估充分性估计器，选择分割深度，上传接口激活与深度信息；服务器完成深度感知投影、原型对齐正则化和预测头更新，返回接口梯度；客户端更新本地骨干和提示令牌。

## 3. 实验设计

### 3.1 数据集与场景

| 数据集 | 类别数 | 特点 |
|--------|--------|------|
| CIFAR-10 | 10 | 通用统计异质性 |
| CIFAR-100 | 100 | 通用统计异质性 |
| Tiny-ImageNet | 200 | 更复杂的视觉语义 |
| DomainNet | 345 | 域级异质性 + 非IID偏移 |

每个数据集均构造 **IID** 和 **非IID**（Dirichlet分布，α=0.1）两种划分；DomainNet同时反映域级视觉变化和客户端级非IID偏移。

### 3.2 骨干网络

- **ResNet-18**（卷积架构）
- **ViT-B/16**（Transformer架构）

### 3.3 对比方法

- **FL基线**：FedAvg、FedProx、FedBABU、FedGH、FedAS
- **SFL基线**：SplitFed、MergeSFL、FedMut、MU-SplitFed、MultiSFL（主要自适应SFL基线）

### 3.4 关键设置

- 客户端数 N=20，全部参与每轮通信；局部训练轮数 L=5，批量大小 64。
- CIFAR-10训练100轮（lr=0.1）；CIFAR-100、Tiny-ImageNet、DomainNet训练300轮（lr=0.01）。
- 默认超参数：ρ=0.1, β=0.05, λ=0.1, τ_a=0.5, τ_m=0.5, m=4, μ=0.9，每5轮校准一次。

## 4. 资源与算力

论文中**未明确说明**所使用的GPU型号、数量及具体训练时长。仅在效率分析中报告了客户端侧的计算开销指标（参数数量、FLOPs、训练时间），但这些是在特定配置下的相对比较，不涉及硬件配置信息。

## 5. 实验数量与充分性

论文的实验覆盖较为全面：

- **主结果对比（RQ1）**：4个数据集 × 2种划分（IID/非IID）× 2种骨干 = **16组主实验**，对比10种基线方法。
- **消融研究（RQ2）**：针对三个模块（Adapt.、Suff. Est.、Harmon.）在4个数据集 × 2种骨干上进行了多组消融，验证了每个组件的贡献。
- **超参数敏感性（RQ3）**：考察了ρ、β、λ、τ_a四个关键超参数在CIFAR-100和DomainNet上的影响。
- **计算效率分析（RQ4）**：与MultiSFL在参数、FLOPs、时间和精度上的对比。
- **分割策略对比（RQ5）**：与静态分割和随机分割策略的比较。

**充分性评估**：实验整体设计较为充分，覆盖了多种异质性场景和两种骨干架构，消融实验验证了各组件的必要性。所有方法使用相同的数据划分、骨干架构和训练协议，保证了比较的公平性。不足之处在于：未提供收敛曲线分析、隐私保护讨论（提示令牌和深度信息共享是否存在隐私风险）以及更大规模客户端（如100+）场景的验证。

## 6. 主要结论与发现

1. **FedSGA在所有数据集、划分方式和骨干架构下均取得最优精度**，且在非IID设定下优势更为明显，说明充分性引导的自适应分割能有效应对客户端异质性。
2. **三个模块均发挥积极作用**：浅层充分性估计器在分割选择中起核心作用；客户端适应通道在非IID和DomainNet设定下增益显著；接口协调模块能有效缓解异构分割深度带来的接口失配。
3. **效率与性能可实现良好平衡**：相比MultiSFL，FedSGA在降低参数激活量、FLOPs和训练时间的同时取得更高精度，收益来自客户端特定的早期离线和避免不必要的深层本地计算。
4. **超参数存在合理区间**：ρ=0.1、β=0.05、λ=0.1、τ_a=0.5附近性能最佳，显示充分性估计和接口协调需要平衡配置。

## 7. 优点

- **问题视角新颖**：将分割点选择从资源驱动重新定义为充分性驱动的决策问题，揭示了静态分割在客户端异构下的根本缺陷。
- **轻量级适应追踪**：利用私有提示令牌作为适应状态探针，避免了全特征空间的复杂追踪，计算开销极小（O(m)）。
- **避免穷举探测**：通过稀疏校准学习充分性估计器，将深度比较成本从O(KT)摊销为O(|T_cal|)，兼顾决策质量与效率。
- **深度感知接口协调**：将深度嵌入与原型对齐相结合的投影设计，合理解决了异构分割深度下的表示可比性难题。
- **架构通用性**：在CNN（ResNet）和Transformer（ViT）上均验证了有效性，方法不依赖特定骨干。

## 8. 不足与局限

- **硬件资源信息缺失**：未报告GPU型号、数量等算力配置，影响实验可复现性和实际部署参考价值。
- **缺乏收敛性理论分析**：论文主要依赖实验验证，未提供自适应分割和充分性估计器在收敛性方面的理论保证。
- **标定误差风险**：浅层充分性估计依赖校准监督，若校准轮次的参考深度选择不当或校准频率过低，可能影响估计精度；β、ρ等阈值参数需要数据集特定调优。
- **隐私泄露隐忧**：客户端上传分割深度信息、深度嵌入和原型对齐统计量，可能向服务器泄露客户端数据分布或训练状态的潜在线索，论文未对此展开安全性讨论。
- **规模验证有限**：仅在20个客户端的设定下验证，未考虑更大规模客户端、部分参与（partial participation）或通信受限等实际场景。
- **异构性覆盖范围**：主要关注标签偏移和域偏移，未涵盖特征偏移、概念偏移等其他异质性类型。

（完）
